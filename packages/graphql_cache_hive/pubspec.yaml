name: graphql_cache_hive
description: An independent cache package for graphql flutter.
version: 0.0.1
authors:
  - Maina Wycliffe <me@mainawycliffe.dev>

homepage: https://github.com/zino-app/graphql-flutter/tree/master/packages/graphql_cache_hive

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: "^1.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: graphql_cache_hive.graphql_cache_hive
    pluginClass: GraphqlCacheHivePlugin
